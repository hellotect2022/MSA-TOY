<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | METABUS</title>

    <style>
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f5f5f5;
}

.login_container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

.login_wrap {
    background-color: white;
    border: 1px solid #ccc;
    border-radius: 10px; /* 테두리의 모서리를 둥글게 만듭니다 */
    box-shadow: 0px 0px 20px 0px rgba(0, 0, 0, 0.2);
    list-style: none;
    padding: 30px; /* 여백을 더 넓게 조정합니다 */
    width: 80%; /* 템플릿 너비를 더 넓게 조정합니다 */
    max-width: 500px; /* 최대 너비를 설정합니다 */
    text-align: center;
}

.login_wrap img {
    width: 100%;
    height: auto;
    max-width: 500px;
    display: block;
    margin: 0 auto;
}

.loginbox02 {
    margin-top: 20px;
}

.hgroup {
    text-align: center;
    margin-bottom: 20px;
}

.login_form {
    text-align: center;
}

.field {
    margin-bottom: 20px; /* 여백을 더 넓게 조정합니다 */
}

.field label {
    display: block;
    margin-bottom: 10px; /* 여백을 더 넓게 조정합니다 */
    font-weight: bold;
}

.inp_text {
    width: 100%;
    padding: 12px; /* 내용 입력 영역의 높이를 더 높게 조정합니다 */
    font-size: 18px; /* 텍스트 크기를 더 크게 조정합니다 */
    border: 2px solid #ccc;
    border-radius: 5px;
}

.btn_login {
    margin-top: 30px; /* 여백을 더 넓게 조정합니다 */
}

.loginBtn {
    display: inline-block;
    padding: 15px 30px; /* 높이와 너비를 더 크게 조정합니다 */
    background-color: #555; /* 회색 배경색을 설정합니다 */
    color: white;
    border: none;
    border-radius: 10px; /* 테두리의 모서리를 둥글게 만듭니다 */
    font-size: 20px;
    cursor: pointer;
    text-decoration: none;
    transition: background-color 0.3s ease; /* 배경색 변경에 트랜지션 효과를 추가합니다 */
}

.loginBtn:hover {
    background-color: #333; /* 마우스 호버 시 배경색 변경 */
}

    </style>
    <script src="/js/jquery-1.9.1.js"></script>
    <script src="/js/custom/custom.js">
        $(document).ready(function(){
            FN.dialogShow()
            $(".loginBtn").click(function(){
                FN.login();
            });
        });
    </script>

</head>

<body>
{{> loading}}
<div id="login" class="login_container">

    <!-- login_wrap -->
    <ul class="login_wrap">
        <li><img src="/images/logo_sc122.png" style="width:500px; height:250px; padding-top:100px;"/></li>
        <li class="loginbox02">
            <div class="hgroup">
                <h1><img src="/images/logintitle.png" alt="Login"/></h1>
            </div>
            <form name="" method="" action="">
                <fieldset>
                    <legend>로그인 폼</legend>
                    <div class="login_form">
                        <p class="field">
                            <label for="uid">이메일을 입력하세요</label>
                            <input name="uid" type="text" id="email" class="inp_text"
                                   onkeypress="if(event.keyCode==13){FN.login();}"/>
                        </p>
                        <p class="field">
                            <label for="pwd">비밀번호를 입력하세요. </label>
                            <input name="pwd" type="password" id="userPassword" class="inp_text"
                                   onkeypress="if(event.keyCode==13){FN.login();}"/>
                            <!-- Modify. 2017. 05. 19 JBum -->
                        </p>

                        <p class="btn_login">
                            <a class="loginBtn"><span class="blind">로그인</span></a>
                        </p>
                        <p class="login_line">
                        </p>
                    </div>
                </fieldset>
            </form>
        </li>
    </ul>
</div>
{{> footer}}
</body>

</html>
